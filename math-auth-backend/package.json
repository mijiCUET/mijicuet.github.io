{
  "name": "math-auth-backend",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "audit": "npm audit --audit-level=moderate",
    "security-check": "node scripts/security-check.mjs",
    "verify": "npm run typecheck && npm run security-check"
  },
  "dependencies": {
    "hono": "4.12.32"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "wrangler": "4.114.0"
  }
}
